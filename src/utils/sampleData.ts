export interface SampleXmlItem {
  id: string;
  name: string;
  category: string;
  description: string;
  xml: string;
}

// All sample invoices removed as requested
export const SAMPLE_XML_FILES: SampleXmlItem[] = [];
